# npm-bundles (Framer)

Host these files on **framer-university/components** under `npm-bundles/` so jsDelivr URLs in code components resolve.

- **cobe.js** — [COBE](https://cobe.vercel.app/) WebGL globe (build from `custom-bundle-gsap/cobe-bundle/`).

After adding to GitHub, this import works in Framer:

`https://cdn.jsdelivr.net/gh/framer-university/components/npm-bundles/cobe.js`
